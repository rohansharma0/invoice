export default function ToolBar() {
    return (
        <div className="h-10 border-b bg-gray-100 flex items-center px-4 text-sm text-gray-600">
            <span>Toolbar (Merge, Export, etc.)</span>
        </div>
    );
}
